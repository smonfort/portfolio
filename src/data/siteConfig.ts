/**
 * Site-wide feature flags and configuration.
 * Toggle these values to enable or disable specific features.
 */
export const siteConfig = {
  /**
   * When true, displays the "Open to work" banner in the Hero section.
   * Set to false to hide it when not looking for new opportunities.
   */
  openToWork: false,
} as const;
